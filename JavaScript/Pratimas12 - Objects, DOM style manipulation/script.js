// Parasyti funkcija kuri per parametrus priima bet koki objekta.
// Per console turi isvesti key values


// const person = {
//     firstName: "John",
//     lastName: "Johnson"
// }

// function fn(param) {

//     const keys = Object.keys(person);

//     for(i = 0; i < keys.length; i++){

//         const keysContent = keys[i];
//         console.log(person[keysContent])
//     }

//     const keyValue = Object.values(person);
//     return keyValue;
// }

// console.log(fn(person))
// ------------------------------------------------------------------------------------------

// Parasyt funkcija kuri per parametrus priema bet koki object,
// grazina nauja objekta kurio "keys" bus seno objekto "values", 
// o "values" bus seno objecto "keys".

// ------------------------------------------------------------------------------------------
// ------------------------------------------------------------------------------------------
// ------------------------------------------------------------------------------------------
